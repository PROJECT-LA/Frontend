export interface TemplatesData {
  id: string;
  name: string;
  description: string;
  version: string;
  status: string;
}

export interface CUTemplateData {
  id: string;
  name: string;
  description: string;
  version: string;
}
